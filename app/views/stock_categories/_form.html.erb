<%= form_for(@stock_category) do |f| %>
  <% if @stock_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stock_category.errors.count, "error") %> prohibited this stock_category from being saved:</h2>

      <ul>
      <% @stock_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
